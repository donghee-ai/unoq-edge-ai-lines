"""Servo Calibration — Python placeholder.

이 App은 sketch.ino만 실행해 서보를 90°로 정렬합니다.
Python 측은 단순히 살아만 있으면 됨 (App Lab 매니페스트 호환).
"""
import time

print("Servo Calibration App — Python side idle.")
print("Sketch is running on STM32U585. Watch logs for [STATUS] messages.")

while True:
    time.sleep(60)
