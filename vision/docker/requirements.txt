# Core scientific
numpy==1.26.4
opencv-python-headless
pyyaml
tqdm
pillow

# TensorFlow / Keras stack (Ultralytics TFLite export 호환 조합)
# 초기 tensorflow==2.13.0 핀은 Ultralytics(8.4.75) export가 tf_keras<=2.19.0 요구해서 깨짐
# → AutoUpdate가 환경 손상. 사전에 2.19 stack으로 핀하여 안정성 확보 (docs/00 환경 진화 섹션 참조)
tensorflow==2.19.1
tf_keras==2.19.0
keras==3.12.2
ai-edge-litert
protobuf>=5,<6
ml-dtypes

# Ultralytics + ONNX 변환 도구 체인
ultralytics
onnx
onnxruntime
onnxslim
onnx2tf
onnx_graphsurgeon
sng4onnx
