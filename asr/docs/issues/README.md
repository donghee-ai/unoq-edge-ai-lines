# Issues — 이슈/문제/오류/함정 실시간 기록

본 작품 진행 중 발생한 **문제 / 오류 / 의외 동작 / 함정**의 발견·진단·해결을 실시간 기록.

## 파일명 규칙

```
<YYYY-MM-DD>_<NN>_<짧은_제목>.md
```

- `NN` = 그날의 일련번호 (01, 02, ...)
- 짧은_제목 = 영문 lowercase + underscore (예: `python_not_found`, `usb_audio_sample_rate`)
- 예: `2026-06-25_01_python_not_found.md`

## 작성 형식 (각 이슈 1 파일)

```markdown
# <한 줄 제목>

## 발생 시각
YYYY-MM-DD HH:MM (선택)

## 증상
- 어떤 명령/작업 중 발생했는가
- 출력 메시지 / 스택트레이스 (그대로 복사)

## 원인
진단 결과

## 해결
어떤 명령 / 코드 수정으로 해결됐는가

## 재발 방지
향후 같은 함정 회피 방법 (사전 점검 / 코드 패턴 / 환경 설정)

## 관련
- 관련 issues / history / docs 파일 링크 (선택)
```

## 기록 시점

**일이 생기는 즉시**:
- Python 오류 / 명령 실패 / 의외 동작 발견
- 환경 차이 / 호환 문제 발견
- 시간 잡아먹은 함정 해결 후

**배치 금지** — 한꺼번에 몰아 기록 X. 발견 즉시 1 파일.

## 본 작품 진행 중 이미 발견된 이슈 8건

지금까지 발견된 이슈는 [`../docs/03_implementation_log.md`](../docs/03_implementation_log.md) §6 통합 정리됨. 향후 발견 이슈는 본 폴더에 1 사건 1 파일로 기록.
